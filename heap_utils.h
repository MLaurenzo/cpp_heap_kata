#ifndef CPP_HEAP_KATA_HEAP_UTILS_H
#define CPP_HEAP_KATA_HEAP_UTILS_H

#include <vector>

void print(const std::vector<int>& v);

void heapify(std::vector<int>& v);
void sort(std::vector<int>& v);

#endif //CPP_HEAP_KATA_HEAP_UTILS_H
